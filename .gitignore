# 依赖与构建产物
node_modules/
npm-debug.log*
package-lock.json
yarn.lock
pnpm-lock.yaml

# 运行期产物
logs/
*.log
data/

# 运行时生成的配置（示例配置在 examples/ 里，受版本管理）
/llm-session-proxy.config.json

# 本地诊断脚本与临时备份（保留目录本身，内容不入库）
.workbuddy/tmp/

# 编辑器与系统
.DS_Store
Thumbs.db
.idea/
.vscode/
