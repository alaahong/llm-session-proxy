# Roadmap（路线图）

`llm-session-proxy` 是一个**零依赖、本地、单进程**的 LLM 反向代理。它只解决一件事，并且要解决得好：
客户端发不出上游要求的请求头。下面所有内容都必须符合这个形状。

- **范围**：一台开发机、一个上游（v0.4 起支持多个）、一个进程。
- **不可妥协**：`package.json` 里的 `dependencies` 永远为空。需要第三方库的功能，要么用 Node 内置模块
  重新实现，要么不上。
- **当前状态**：v0.2.x —— 会话与请求头注入的核心已可用且做过加固；模型别名、`--dry-run` / `--doctor`、
  变换注册表、路由分桶与协议互转都已交付。其余都在这一页上。

---

## 1. 现在到哪了

### 已经能用（v0.2.2）

| 方面 | 现状 |
| --- | --- |
| 会话 | 三级策略：客户端显式标识 → 内容指纹（`system` + 首条 user 消息）→ 一次性随机。同一对话内稳定，上游的提示词缓存才有意义。 |
| 注入 | 任意请求头与请求体字段，支持模板（`{{session.id}}`、`{{uuid}}`、`{{env.HOME}}` 等）、点路径与 `overwrite` 开关。 |
| 重写 | 模型别名（精确映射 + 前缀剥离，两者可叠加）与请求路径（正则规则）。OpenCode Go 的别名表**已内置**，所以 README 让你写的 `proxy-` 前缀不加配置文件也能解析；别名剥完前缀查不到映射时会点名告诉你，而不是悄悄转发。 |
| 路由 | 四个桶（`default` / `background` / `think` / `longContext`），各自可以换模型、挂变换。规则按路径前缀、模型前缀、请求体字段、请求体大小匹配，**自上而下、首个命中即止**，同一条规则内条件是与。**默认关闭**，升级不会改变现有行为。 |
| 变换 | 五个内置命名变换（`noop`、`drop-fields`、`drop-empty-fields`、`rename-fields`、`clamp-max-tokens`），可全局挂也可按桶挂，按固定顺序执行。**按名字选，绝不按路径选**，所以配置文件无法让代理执行代码。 |
| 协议 | Anthropic Messages ↔ OpenAI Chat Completions ↔ OpenAI Responses：请求体转发前整体转换，JSON 响应整包改写，SSE 流**按事件逐条**转码、流式体验不降级。默认关闭，按模型前缀路由；上游 4xx/5xx 错误体永不转换。 |
| 体检 | `--dry-run` 打印生效的路由、**渲染后**的注入表、模型解析链路与分桶/变换决定，不产生任何网络请求；`--doctor` 另加 DNS/TCP/TLS 可达性与监听端口检查，有问题时退出码非 0。 |
| 流式 | SSE 逐块管道转发，从不整体缓冲。上游 4xx 响应体原样回传。 |
| 运维 | `/__llm_session_proxy__/status` 与 `/sessions` 本地端点、**默认落盘**的日志（`~/.lsp/logs`，支持按大小/按日期轮转与 30 天归档）、四层配置合并（默认值 < 配置文件 < 环境变量 < 命令行）。 |
| 稳定性 | 单个畸形请求打不死进程；未捕获异常会落进日志文件；**281 个测试**，其中 9 条专攻畸形输入与半开连接（非法 `Host`、上游 reason phrase 与响应头里的控制字符、畸形请求行、超大请求头、请求体没读完就断开、上游掐掉空闲 keep-alive）。 |
| 语言 | 控制台与日志文案**默认英文**，`--lang zh` / `PROXY_LANG=zh` 切中文。 |

### 已知缺口（直说）

1. **只有一个上游，没有兜底。** 没有重试、没有健康检查、没有熔断。
2. **没有机器可读的可观测性。** 只有给人看的日志行：没有指标端点、没有结构化日志模式、没有 trace 导出。
3. **变换注册表是写死的清单。** 只有五个内置变换，而且没有办法从路径加载一个。变换层面仍然缺的是：
   system 提示词**内容**改写（协议互转会搬运 system 消息的形状，但不改写它说了什么）。
4. **没有缓存，也没有成本统计。** 我们能帮**上游**命中缓存，但报不出来有没有命中。
5. **协议互转在边缘处刻意有损。** Anthropic 的 `thinking` / `signature` 流增量转 chat 时丢弃；
   `messages ↔ responses` 经 chat 中转。

---

## 2. 同类项目教了我们什么

这个品类分成五族，我们刻意待在最小的那一族里。

| 项目 | 运行时 | 是什么 | 比我们强在哪 | 我们借鉴什么 | 我们刻意不做 |
| --- | --- | --- | --- | --- | --- |
| **[LiteLLM](https://github.com/BerriAI/litellm)** | Python | 自托管网关的默认选择：100+ 供应商，统一成 OpenAI 形状 | 供应商广度、虚拟密钥、预算、按团队统计花费、负载均衡 | 配置面的**形状**；以及「客户端真正想要的是一个 OpenAI 兼容端点」这条结论 | 虚拟密钥、预算、数据库、管理面板——那都是多租户 SaaS 的机械结构 |
| **[claude-code-router](https://github.com/musistudio/claude-code-router)** | Node.js | Claude Code 的本地路由：分桶（`default` / `background` / `think` / `longContext`）、transformer 插件体系、Anthropic↔OpenAI 互转 | 协议转换、规则/分桶路由、按供应商的 transformer、Web UI | 分桶路由与「transformer 注册表作为配置概念」；`longContextThreshold` 这类策略 | 它那套插件加载模型，以及只服务单一客户端的定位 |
| **[one-api](https://github.com/songquanpeng/one-api) / [new-api](https://github.com/Calcium-Ion/new-api)** | Go | 中转面板：多渠道、额度、令牌计费，对国内供应商友好 | 渠道池、额度与计费、管理界面 | 结构上不借鉴——它是服务，我们是 CLI | 整套计费/额度模型 |
| **[gpt-load](https://github.com/tbphp/gpt-load)** | Go | 上游 key 池 + 负载均衡 | key 轮换、按权重均衡 | key 池轮换（v0.4，前提是能保持纯文件配置） | 数据库、Web 管理后台 |
| **[Portkey](https://github.com/Portkey-AI/gateway) / [Bifrost](https://github.com/maximhq/bifrost) / [Envoy AI Gateway](https://github.com/envoyproxy/ai-gateway)** | Node / Go / Envoy | 为吞吐、护栏与治理而生的生产网关 | 实测个位数毫秒开销、护栏链、熔断、K8s 原生策略 | 护栏原语的设计、被动健康检查、以及「把基准测试工具一起提交进仓库」的纪律 | Envoy/K8s 的部署重量，以及追逐微秒级头条 |
| **[Cloudflare](https://developers.cloudflare.com/ai-gateway/) / [Vercel AI Gateway](https://vercel.com/docs/ai-gateway) / Helicone / Langfuse** | 托管 | 托管网关与可观测平台 | 零运维、DLP/PII 扫描、成本看板、trace UI | 值得暴露的指标集合（TTFT、附加延迟、token 数、前缀复用） | 把提示词发到任何外部服务 |

### 三条我们认真对待的结论

**1. 公开的网关基准大多是拿 mock 上游测的，数值跨度有五个数量级。** 2026 年公开的数字从厂商自称的
约 11 µs 开销（Bifrost，仅自身压测、mock 上游、不含上游响应时间），到 LiteLLM 的 Python 代理在开启
日志回调时约 40 ms 的中位开销。一份独立对照测试（仓库内置跑法，n=5000，本地确定性 mock，非流式，
关掉回调）给出的 p99 附加延迟是：LiteLLM 的 Rust 重写约 0.7 ms、Portkey 约 2.3 ms、Bifrost 约 4.5 ms，
而 LiteLLM 的 Python v1 约 257.7 ms。唯一一次拿**真实**上游测的公开结果是 Envoy AI Gateway 2026 年
7 月的测试：在流式、对接真实 GPU 推理的前提下，附加开销约 2 ms，落在 20 ms 预算之内。

对我们的结论是：**把跑法一起开出来、写清测量口径，然后别再争微秒。** 供应商的 TTFT 是 500 ms 到 5 s，
网关开销属于舍入误差。所以第 4 节给的是可测量、可复现的目标，第 7 节说清怎么测。

**2. 功能广度赢不了，而且那不是重点。** LiteLLM 有 100+ 供应商集成和一整个团队。我们是一个进程对一个
上游加一个配置文件。我们的优势只能建立在**小处的保真度**上：正确地绑定对话、让上游的提示词缓存保持命中、
恰好注入上游要的那些头、把字节原样透传。

**3. 供应链面积本身就是一项功能。** LiteLLM 在 2026 年发布了 12 份安全公告，其中包括一个预认证 SQL 注入
和一个未认证 RCE（后者进了 CISA 的已知被利用漏洞清单），两个都在当前稳定版修复；另外还有两个 PyPI 版本
短暂地投放过窃取云凭据的代码。这就是「一个对网络暴露、带数据库和管理面板的服务」的代价。我们的答案是
结构性的，而不是流程性的：**零运行时依赖、默认只听回环、没有管理面板、没有会做错的入站鉴权、没有需要打补丁
的东西。** 任何会侵蚀这一点的路线图条目，无论多受欢迎，都会被否掉。

---

## 3. 约束与非目标

**永久的非目标：**

- ❌ 多租户虚拟密钥、额度、计费。那是 one-api / new-api / LiteLLM 的地盘。
- ❌ 模型服务端。我们不做推理；vLLM 和 Ollama 已经在做了。
- ❌ 数据库。会话和价格放在内存或小文件里。
- ❌ 需要构建步骤的管理界面。真要做看板，就是同一个 Node 进程直接吐手写 HTML/JS。
- ❌ 默认绑 `0.0.0.0`。代理会转发你的 API key，它就得是个本地工具。
- ❌ 换非 Node 运行时或 Rust 重写。启动速度和 `npx` 本身就是产品。
- ❌ 追逐微秒级头条。我们优化「不成为瓶颈」，并且把测试跑法一起公开。

**约束：**

- 永远零运行时依赖。HTTP/2、TLS、指标文本格式、JSON 日志和看板都能用 Node 内置模块做到；
  做不到的就不上。
- 单进程、单端口。
- 每个新开关都要带：测试、两份 README、文档站中英两页，以及这一页上的勾。

---

## 4. 性能目标

由第 7 节的跑法测量，环境为 4 vCPU / 16 GB Linux 虚拟机；除特别说明外，上游是**本地 mock**。
测量口径与数字一起发布——没有口径的数字没有意义。

| 指标 | v0.2 目标 | v1.0 目标 |
| --- | --- | --- |
| 附加延迟，非流式（p50） | ≤ 1.0 ms | ≤ 0.6 ms |
| 附加延迟，非流式（p99） | ≤ 5 ms | ≤ 2.5 ms |
| 附加首字节时间，流式（p95） | ≤ 2 ms | ≤ 1 ms |
| 吞吐，100 并发连接 | ≥ 1,200 RPS，0 错误 | ≥ 2,500 RPS，0 错误 |
| 常驻内存，1,000 条打开流 | < 90 MB | < 60 MB |
| SSE 透传中被改动的字节 | 0（逐字节一致） | 0 |
| 模糊测试语料下的进程退出次数 | 0 | 0 |
| 冷启动：`npx` → 第一个被服务的请求 | < 2.0 s | < 1.5 s |

同类参照（仅用于校准）：2026 年那次独立对照里，正经网关对 mock 上游的 p99 落在约 0.7–4.5 ms。
p99 进入 5 ms 以内就算进了这个区间；而我们真正在卖的是**零依赖 + 本地**。

---

## 5. 里程碑

### v0.2 — 正确性与协议覆盖 <sub>（「一个客户端吃满所有模型」）</sub>

这一页上价值最高的功能。现在客户端的协议决定了它能用到哪些模型；v0.2 之后这个限制消失。

**分三批交付。** v0.2.0 先拿走不需要新架构的部分；v0.2.1（下面打勾的两项）补上转换器所依赖的路由层；
v0.2.2 交付协议转换本身。这样切分的理由：双向 SSE 转码是这一页里风险最高的改动，把它和路由改动分开、
单独 bisect，比两件事混在一起排查容易得多。

- [x] **协议转换：** Anthropic Messages ↔ OpenAI Chat Completions ↔ OpenAI Responses，双向、含流式，
      三个端点地位对等，而不是只有一个碰巧能跑。
      <sub>**v0.2.2 已完成。** `src/protocol.js`（识别与路由）、`src/converters.js`（请求体）、
      `src/replies.js`（JSON 响应）、`src/stream.js`（SSE 转码）。SSE 的中间形式选了 chat 的
      `chat.completion.chunk`——三种协议里增量粒度最细——于是每侧只需要一个状态机。转换在管线里
      **最后**执行，排在变换与注入之后；转换生效时会强制上游 `accept-encoding: identity`，因为 gzip
      流没法按 SSE 事件边界切开。</sub>
- [x] **转换中的字段归一化：** `thinking` ↔ `reasoning_effort`、`max_tokens` ↔ `max_completion_tokens`、
      工具调用形状、`cache_control` 处理、stop 序列的类型差异。
      <sub>**v0.2.2 已完成。** 预算固定表 low=1024 / medium=8192 / high=16384，反向按阈值反推。
      没有等价物的字段（`cache_control`、`response_format` 等）逐个**记名**丢进 `dropped` 列表；
      Anthropic 必填的 `max_tokens` 缺失时补一个记入日志的 4096，而不是等上游 400。错误体
      （4xx/5xx）按设计豁免转换。</sub>
- [x] **Transformer 注册表：** 按上游可选、具名的变换（参考 claude-code-router 的做法），实现放在仓库内。
      <sub>**v0.2.1 已完成。** `src/transformers.js` 内置五个：`noop`、`drop-fields`、`drop-empty-fields`、
      `rename-fields`、`clamp-max-tokens`，每个都声明了 `phase`，好让响应侧的转换器在 v0.2.2 有位置落。
      **上面写的「用户显式开启即可加载插件」被有意砍掉了**：对一个以「零依赖、只听回环」为卖点的工具，
      让配置文件指向代码是不成比例的攻击面。名字不认识就是启动报错，不静默当无事发生。</sub>
- [x] **路由分桶：** `default` / `background` / `think` / `longContext`（带阈值），外加规则匹配
      （路径前缀、模型前缀、请求体字段、请求体大小）。纯配置实现。
      <sub>**v0.2.1 已完成。** 见 `src/router.js`，另加 `--router` / `--transformer` / `--no-router`
      三个参数，以及 `--dry-run` / `--doctor` 里的 `路由分桶` 与 `变换` 两个区块。阈值一律按**字节、不按
      token**：估 token 就得带一个分词器，而字节数是你能实际调得动的数字。`router.enabled` 默认 `false`，
      一条配置测试加一条端到端代理测试钉住「升级不改行为」。**上面写的「用户可以指向自己的 JS 路由模块」
      同样被砍掉了，理由和插件加载一样。**</sub>
- [x] **修掉 `model.map` 缺口：** 为常见上游提供一份默认映射；当 `proxy-` 前缀的别名解析不到任何东西时
      大声告警；让 README 与实际行为对齐。
      <sub>**v0.2.0 已完成。** `src/models.js` 内置 27 条别名；告警会点名该补哪一条，且每个别名每进程
      只喊一次；两份 README 都补上了「解析顺序」与「合并语义」。修的过程中还揪出一个潜伏的引用共享
      缺陷——`buildConfig` 通过浅拷贝就地改写了 `DEFAULT_CONFIG`，调用方改动自己的配置会污染进程级
      默认值。已改为深拷贝，并加了测试钉住。</sub>
- [x] **`--dry-run` / `doctor`：** 校验配置、解析一个示例模型、打印生效的路由与注入表、探测上游可达性，
      有问题时以非零码退出。
      <sub>**v0.2.0 已完成。** 见 `src/doctor.js`。刻意只探到传输层——不发 HTTP 请求、不带凭据——
      所以体检不会消耗上游速率配额，也不会碰到密钥。</sub>

### v0.3 — 可观测与可控

- [ ] **Prometheus 文本格式的 `/metrics`**（挂在本地前缀下）：按上游/状态码的请求数、附加延迟直方图、
      会话表大小、缓存复用计数、进出字节数。
- [ ] **结构化日志：** `--log-format json`，字段名稳定，一请求一 JSON 对象，无需再写解析器就能 grep 与投递。
- [ ] **OTLP/JSON 导出**到用户配置的 HTTP 端点（用 JSON 编码而非 protobuf，以保持零依赖）。默认关闭。
- [ ] **会话与前缀检查器：** 暴露每个会话的稳定前缀哈希，并估算上游的提示词缓存是否可复用——这正是这个
      工具存在的意义所在的指标。
- [ ] **token 与成本统计：** 按会话/模型/上游统计，价格表由用户提供。我们不会硬编码厂商价格——变得太快。
- [ ] **免构建看板：** 一个手写 HTML 页，本地起，读本地端点。没有打包器、没有框架、不需要 npm install。

### v0.4 — 真实上游下的可靠性

- [ ] **多上游：** 支持优先级与按权重选择。
- [ ] **兜底与重试：** 对 429/5xx/超时做带抖动的重试并有硬性次数上限；对已经部分吐出流的请求绝不重试。
      （2026 年的生产遥测调研把大约三分之一的 LLM API 错误归因于限流——真实世界的可用性主要就从这里来。）
- [ ] **熔断与被动健康检查：** 连续失败即跳闸，用单个请求探测恢复，熔断状态在 `/status` 上可见。
- [ ] **超时拆分与流空闲看门狗：** 连接 / 响应头 / 空闲 / 总超时分开，空闲看门狗在流卡住时以明确的 504
      中断，而不是永远挂着。
- [ ] **优雅退出：** 收到 `SIGTERM` 后停止接收新请求、把在途 SSE 跑完，再退出。
- [ ] **可选的会话持久化：** 一个小型追加文件，让重启不打断对话绑定。默认关闭。

### v0.5 — 安全与卫生

- [ ] **日志中的凭据卫生：** 永不记录凭据值（现在只记录有/无——保持住），并对形似密钥的串做一次脱敏。
- [ ] **护栏：** 基于正则的输入/输出掩码，以及按上游的请求头/请求体白名单。
- [ ] **配置 schema 校验：** 报错精确到行并附带说明，schema 写成文档。
- [ ] **CI 用的 `--check-config`：** 同一套校验，以退出码形式暴露，方便把代理配置纳入版本管理的人。

### v1.0 — 分发与承诺

- [ ] **单文件可执行产物**（Node SEA），覆盖 Linux/macOS/Windows，外加 distroless Docker 镜像、
      Homebrew 与 Scoop 安装源。`npx` 仍是主要入口。
- [ ] **客户端兼容性矩阵**，每个客户端一份随仓库的 fixture：Trae、Cursor、Cline、Continue、Aider、
      Claude Code、OpenCode、Zed、Roo。
- [ ] **按协议的契约测试**，用 golden SSE fixture，包含畸形流与截断流。
- [ ] **配置迁移工具**（`--migrate-config`），让升级不会把人卡在半路。
- [ ] **v1.0 起承诺语义化版本：** 破坏性配置变更必须带迁移路径。

---

## 6. 优先级

按（覆盖面 × 差异化）÷ 工作量 排序。

| 条目 | 覆盖面 | 差异化 | 工作量 | 优先级 |
| --- | --- | --- | --- | --- |
| 协议转换 | 极高 | 高 | 高 | ✅ v0.2.2 已交付 |
| `model.map` 默认值与文档修复 | 高 | 低 | 极低 | ✅ v0.2.0 已交付 |
| `/metrics` + JSON 日志 | 高 | 低 | 低 | **P1** |
| doctor / dry-run | 高 | 中 | 低 | ✅ v0.2.0 已交付 |
| Transformer 注册表 + 路由分桶 | 高 | 中 | 中 | ✅ v0.2.1 已交付 |
| 会话与提示词缓存检查器 | 中 | **极高** | 中 | **P1** |
| 兜底 / 重试 / 熔断 | 高 | 低 | 中 | **P2** |
| 流空闲看门狗 | 中 | 中 | 低 | **P2** |
| 看板 | 中 | 低 | 中 | **P3** |
| 成本统计 | 中 | 低 | 中 | **P3** |
| 单文件产物 | 低 | 低 | 高 | **P3** |

---

## 7. 怎么测

`scripts/bench.mjs`（v0.2.x 后期、协议层不再大改之后再加入）会：

1. 起一个延迟与流式都可配置的确定性 mock 上游；
2. 用固定请求语料、在指定并发下分别直连上游和经代理各跑一遍；
3. 报告按分位的附加延迟、TTFT 差值、吞吐、常驻内存、错误率与 SSE 字节一致性；
4. 把 `bench/results/<version>.json` 写进仓库，内含 CPU 型号、Node 版本、操作系统与完整命令行参数。

使用规则：

- 拿 mock 测就必须说明是拿 mock 测的，报数字必须把那一跑的元信息放在旁边。
- 让基准变好但让字节一致性测试挂掉的改动是回归，不是收益。
- 基准在 CI 里只做趋势观察，只有稳定性语料能阻断合并。

---

## 8. 工程原则

1. **零运行时依赖。** 需要依赖的功能，要么重新实现，要么否掉。
2. **能管道转发就不要缓冲。**
3. **别把策略评估放进关键路径。** 护栏与统计和流并排跑，一个慢策略绝不能拖慢一个 token。
4. **请求路径上软失败，日志里硬失败。** 一个坏请求变成 4xx；一个 bug 变成一行日志，而不是退出。
5. **宁可删掉一个配置开关，也别新增一个。** 常见场景的默认值就应该是正确的。
6. **文档属于「完成」的定义：** 两份 README、站点两页、以及这一页。
7. **默认只听回环。** 任何让代理能从网络访问到的改动都是显式可选，并按风险写进文档。

---

## 9. 节奏与版本

- `v0.x` 可能调整配置结构；每个版本都会说明哪些字段挪了位置。
- 一个里程碑可以拆成多个小版本，当各部分的**风险等级不同**时就应该拆。v0.2 是头一个例子：
  v0.2.0 交付映射表缺口与 doctor，v0.2.1 交付路由，v0.2.2 交付协议转换。每个小版本都能独立安装、
  独立验证。
- 从 `v1.0` 起走语义化版本；破坏性配置变更必须配 `--migrate-config`。
- 发布走 `.github/workflows/publish.yml`，打 `v*` 标签触发，发布前先跑打包自检与安装后
  端到端验证。

---

## 10. 不在路线图上（以及为什么）

| 想法 | 为什么不做 |
| --- | --- |
| 虚拟密钥、额度、计费 | 多租户 SaaS 的机械结构；one-api / new-api / LiteLLM 已经做了，而数据库在开发机上是个负债。 |
| 本地跑模型 | vLLM、llama.cpp、Ollama 都在做，而且做得更好。 |
| 托管版本 | 这里的价值恰恰是提示词和密钥留在你自己机器上。 |
| 插件市场 | 从第三方加载任意代码，正是我们要躲开的供应链风险。 |
| K8s operator / sidecar | 目标是一台笔记本。 |
| 为了基准头条改用 Rust/Go | `npx` 起的启动速度和免安装比那 0.5 ms（没人感知得到）值钱。 |
